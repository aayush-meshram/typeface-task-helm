apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typeface-be.fullname" . }}-test-connection"
  labels:
    {{- include "typeface-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "typeface-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
