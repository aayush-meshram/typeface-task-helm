apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typeface-fe.fullname" . }}-test-connection"
  labels:
    {{- include "typeface-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "typeface-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
